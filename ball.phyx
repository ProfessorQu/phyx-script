let x = 0;

fn addCircle(i) {
    objects.add(circle {
        gravity: 3,
        color: hsv(i, 1.0, 1.0),
        x: i - 180,
        y: i - 180
    })
}

for i in range(360) {
    addCircle(i)
}

objects.add(ring {
    size: 300
})

